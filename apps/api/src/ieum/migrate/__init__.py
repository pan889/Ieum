"""이관 어댑터와 묶음 포맷.

**이 꾸러미는 표준 라이브러리만 쓴다.** 어댑터는 관리자의 기계에서 도는
것이고(ADR-0016), 거기에는 Ieum 서버가 깔려 있지 않다. `ieum.*` 의 다른
무엇이든 여기서 부르는 순간 그 전제가 깨진다.

규칙을 사람의 주의에 맡기지 않는다 — `test_migrate_stands_alone.py` 가
이 꾸러미의 임포트를 전수로 훑어 막는다.
"""
