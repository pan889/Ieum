# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/
dist/
*.egg-info/

# Node
node_modules/
# compose 의 web 컨테이너가 저장소를 통째로 마운트한다. pnpm 스토어가
# 작업 트리에 그대로 쌓인다(수백 MB). 아래 볼륨으로 빼 두지만, 예전
# 체크아웃이나 호스트 pnpm 이 여기에 만들 수도 있어 무시한다.
.pnpm-store/
apps/web/dist/
*.tsbuildinfo

# 환경
.env
.env.local

# 생성물
packages/api-client/src/generated/
apps/api/openapi.json

# E2E 산출물
test-results/
playwright-report/

# 로컬 인프라
pgdata/

# 매뉴얼 사이트의 빌드 결과. 소스는 `manual/docs/` 다.
manual/site/
