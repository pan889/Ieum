# CLAUDE.md — 포인터

> **이 파일은 규칙 본문이 아니다.** 개발 규칙의 정본은 문서 저장소가 단독으로 소유한다.
> 여기에 규칙을 복사해 두지 않는다. 복사본은 반드시 원본과 어긋나기 때문이다.

## 세션 시작 시 반드시 할 일

이 저장소에서 작업을 시작하기 전에 **문서 저장소의 `CLAUDE.md`를 먼저 읽는다.**

- 정본: https://github.com/pan889/ieum-docs/blob/main/CLAUDE.md
- 문서 지도 전체: https://github.com/pan889/ieum-docs

읽지 못했다면 (접근 권한 없음 등) 추측으로 진행하지 말고 **사용자에게 알린다.**
`docs/...` 로 시작하는 모든 경로 참조는 이 저장소가 아니라 **문서 저장소 기준**이다.

## 저장소 분리 규칙

| 저장소 | 소유 대상 |
|---|---|
| `pan889/Ieum` (여기) | 코드 — `apps/`, `packages/`, `deploy/` |
| `pan889/ieum-docs` | 개발 규칙(`CLAUDE.md`), `docs/**` — 제품·아키텍처·ADR·로드맵·규약 |

코드 변경이 문서와 어긋나면, 문서 저장소에 **별도 변경**으로 반영한다.
원본 `CLAUDE.md` 5절의 "문서 지도" 표가 어떤 문서를 언제 갱신할지 정한다.
