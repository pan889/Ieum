# 바뀐 것

판 번호는 `MAJOR.MINOR.PATCH` 입니다. 올리는 방법은
[설치 안내](deploy/install/README.md)의 "판 올리기" 에 있습니다.

## 1.0.2 — 2026-09-12

또 고치는 판입니다. 새 기능은 없습니다. 1.0.1 에서 급한 것부터 잡고, 이번에는
**남은 것을 마저** 잡았습니다. 고친 자리를 하나씩 되돌려 시험이 붉어지는 것을
확인했습니다.

### 올리기 전에

마이그레이션은 없습니다. **다만 운영으로 `deploy/compose/prod.yml` 을 쓰고
있었다면 아래 "운영 스택" 을 꼭 읽어 주십시오** — 지금 열려 있는 포트가
있습니다. 그리고 `IEUM_DB_PASSWORD`·`IEUM_S3_ACCESS_KEY`·`IEUM_S3_SECRET_KEY`
가 이제 **반드시 필요합니다**(없으면 뜨지 않습니다).

### 보안

- **운영 compose 가 개발용 포트를 그대로 열어 두고 있었습니다.** 겹치는
  파일에서 `ports: []` 와 `volumes: []` 는 **지우는 것이 아니라 아무것도
  안 하는 것**입니다(Compose 는 목록을 덧붙입니다). 그래서 운영으로 띄운
  스택이 실제로는 호스트 소스 트리를 이미지 위에 덮어쓰고, postgres 5432
  (`postgres:postgres`)·redis 6379(암호 없음)·minio 9000·9001
  (`minioadmin`)·mailpit 8025(모든 메일이 보입니다)를 열고 있었고,
  **아무에게나 토큰을 발급하는 가짜 IdP 가 9099 에서 돌고 있었습니다.**

  `!reset` 으로 실제로 지우고, 웹만 80 을 씁니다. 가짜 IdP 는 운영
  프로필에서 빠집니다. 개발용 기본 비밀번호는 자리에서 없앴고, 실제 값을
  주지 않으면 컨테이너가 뜨지 않습니다. `docker compose config` 로 합쳐진
  결과를 직접 확인하는 시험을 붙였습니다.

- **웹훅이 내부 주소로 나갈 수 있었습니다.** 주소 검사가 `http/https` 인지만
  봤습니다. 전송 기록에는 응답 본문 앞부분이 남고 그건 화면에 그대로
  보이므로, 프로젝트 관리자 권한 하나로 `169.254.169.254`(클라우드 자격
  증명)·같은 망의 Redis·우리 내부 API 를 열어 **읽을** 수 있었습니다.

  이제 보내기 직전에 이름을 풀어 **실제 IP** 를 보고, 확인한 그 IP 로
  바로 붙습니다(이름으로 다시 붙으면 그 사이에 DNS 가 답을 바꿀 수
  있습니다). 사내망 수신처로 보내야 하면
  `IEUM_WEBHOOK_ALLOW_PRIVATE_TARGETS=true` 로 엽니다 — 기본은 막습니다.

- **설치 스크립트가 판을 올릴 때 `.env` 사본을 0644 로 만들었습니다.** 몇
  밀리초지만 같은 호스트의 아무 계정이나 시크릿 키·DB 비밀번호·스토리지
  열쇠를 읽을 수 있었습니다. 원본은 0600 인데 사본만 열려 있었습니다.

- **붙여넣은 남의 트래커 링크가 우리 이슈 링크로 바뀌었습니다.** 호스트를
  안 보고 경로만 봤습니다. `https://redmine.partner.example/issues/ENG-1` 이
  `issue:ENG-1` 로 저장돼 **우리 쪽 ENG-1** 을 가리켰고, 링크는 멀쩡해
  보이는데 원래 주소는 이미 없었습니다.

### 고친 것

- **문서 코멘트 목록이 서버를 통째로 멈췄습니다.** 앵커 재계산이
  `difflib` 인데 이벤트 루프 위에서 돌았습니다 — 20만 자 문서에서 한 건에
  5.3초를 쟀고, 그동안 그 워커가 받아 둔 **다른 모든 요청**이 같이 섰습니다.
  스레드로 내보내고, 창 점수를 상한으로 걸러 한 건의 비용을 6~24배 줄이고,
  한 요청이 퍼지 탐색에 쓰는 총량에 상한을 뒀습니다. 상한에 걸린 코멘트는
  **고아로 단정하지 않습니다** — 안 찾아본 것을 "없다" 로 적으면 멀쩡한
  코멘트에 고아 표시가 붙습니다.

- **웹훅 전송이 DB 트랜잭션을 몇 분씩 붙잡았습니다.** 한 배치가 50건이고
  한 건이 최대 10초입니다. 커넥션이 묶이는 것보다, 열린 트랜잭션이 vacuum
  의 기준선을 붙잡아 테이블 전체의 정리가 밀리는 쪽이 더 나쁩니다. 잡기 →
  보내기 → 적기 세 단계로 나눴습니다.

- **다이제스트가 건수를 거짓말했습니다.** 가져온 줄(스무 개)을 세어서,
  500건 쌓인 사람에게 "21건" 이라 적고 거기에 "그리고 1건 더" 를 붙였습니다.

- **보관된 문서와 같은 제목으로 문서를 만들면 500 이 났습니다.** 새 이름을
  고를 때 보관된 형제를 빼고 봤는데, 유일 인덱스는 빼지 않습니다. 무엇과
  부딪혔는지 말해 줄 수도 없었습니다 — 화면에 안 보이는 행이니까요.

- **최상위 문서가 `blog` 이름을 가져갈 수 있었습니다.** 그러면 그 아래
  하위 문서의 경로가 블로그 글 경로와 글자 그대로 같아지고, 경로로 문서를
  찾는 자리에서 500 이 났습니다.

- **초안으로 써 뒀다가 나중에 공개한 블로그 글에 게시 시각이 없었습니다.**
  목록이 그 값으로 정렬하는데 NULL 이 맨 앞에 오므로, 그 글이 블로그 맨 위에
  박혀서 안 내려왔습니다.

- **동시 편집 방이 샜습니다.** 붙는 도중에 창을 닫으면(흔합니다) 방을 집어
  놓고 놓지 않아, 그 문서의 방이 프로세스가 죽을 때까지 본문과 프레즌스
  상태를 든 채 남았습니다.

- **이관 묶음의 값을 받는 쪽 크기에 맞추지 않았습니다.** 제목이 500자를
  넘거나 진행률이 0~100 밖이면 이슈를 만드는 **도중에** DB 가 거절했고,
  그때는 이미 앞의 수백 건이 들어간 뒤였습니다. 이제 읽는 자리에서 맞추고,
  맞춘 것은 "안 옮겨진 것" 에 적습니다. 코멘트의 빈 `source_id` 는
  거절합니다 — 지금까지는 첫 하나만 넣고 나머지를 **이미 옮긴 것으로 보고
  버렸습니다.**

- **티켓 회신 단추 두 개가 동시에 눌렸습니다.** 회신을 보내는 동안 옆의
  "내부 노트" 가 멀쩡히 눌려서, 같은 글이 회신으로 한 번 내부 노트로 한 번
  올라갔습니다. 고객은 이미 읽었으므로 되돌릴 수도 없었습니다.

- **역할 권한을 잇달아 켜면 앞의 것이 조용히 꺼졌습니다.** 저장이 끝나자마자
  칸이 다시 눌리는데 화면의 목록은 아직 옛 값이라, 다음 칸을 켜면 방금 켠
  것이 빠진 목록이 통째로 저장됐습니다. 준 사람은 준 줄 알고, 받은 사람은
  안 된다고 합니다.

- **보드에서 옮기기가 실패해도 아무 말이 없었습니다.** 알림은 그렸는데 그
  위를 대화상자 덮개가 가리고 있었습니다. 그리고 옮긴 직후 옛 `version` 으로
  다음 이동을 보내 아무도 안 고쳤는데 409 가 났습니다.

- **미리 보기가 늦게 와서 새 파일 자리에 옛 결과를 앉혔습니다.** 이관
  화면에서 파일을 바꾸면 화면은 비워지지만 먼저 보낸 요청은 그대로 돌아옵니다.

- **반복 이슈를 껐는데 안 꺼져도 아무 말이 없었습니다.** 켜고 끄기와
  지우기에 오류 표시가 없었습니다.

- **SLA 에스컬레이션의 퍼센트 칸에 글자를 칠 수 없었습니다.** 값으로 키를
  만들어서 한 글자마다 줄이 부서지고 다시 만들어졌습니다 — `8` 을 치면
  커서가 튕겨 `80` 을 칠 수가 없었습니다. 같은 조건·조치를 두 줄 두면 키도
  겹쳤습니다.

- **표 셀 안의 `|` 가 셀을 갈랐습니다.** 편집기를 한 번 열었다 닫은 것만으로
  그 줄이 한 칸씩 밀리고 넘친 값이 사라졌습니다. 되돌릴 원본은 이미 없습니다.

- **이름에 공백이 있는 첨부는 링크가 끊겼습니다.** `[그림](attachment:
  <id>/보고서 최종.png)` 은 `…/보고서` 까지만 링크입니다. 읽는 쪽은 이미
  되돌리고 있었는데 적는 쪽이 그대로 넣고 있었습니다.

- **SMTP 사용자·비밀번호 자리가 앱에 없었습니다.** 설치본은 처음부터 `.env`
  에 적어 주고 있었는데, 깐 사람은 값을 채우고도 메일이 안 나갔고 화면에는
  아무 말도 없었습니다.

- **새로 까는 사람의 `docker compose up` 이 실패하고 있었습니다.** MinIO 가
  도커 허브 저장소의 익명 받기를 닫았습니다 — `pull access denied for
  minio/minio`. 이미 받아 둔 기계에서는 멀쩡해 보이므로 우리 쪽에서는 아무
  일도 안 일어났고, 깨지는 것은 처음 까는 사람 쪽뿐이었습니다. 같은 태그가
  quay.io 에 **다이제스트까지 같게** 있어서 레지스트리만 바꿨습니다(개발
  스택·설치본·CI 세 곳 모두). 셋이 갈라지지 않도록 시험을 붙였습니다.

### 안에서 고친 것

- **릴리스가 이미지를 밀고 나서 멈추는 자리가 있었습니다.** CHANGELOG 절이
  없으면 노트 단계에서 죽는데, 그때는 ghcr 에 이미지 두 개가 이미 올라간
  뒤입니다. 태그도 릴리스도 없는 이미지가 남고, 고쳐서 다시 돌리면 같은
  태그를 다른 바이트로 덮어씁니다. 이제 **이미지를 밀기 전에** 봅니다.

- **절대 붉어질 수 없는 시험 세 개를 고쳤습니다.** `paused_seconds >= 0`
  (카운터라 늘 참), `age >= 0.0`(값이 깎여 나오므로 늘 참), 그리고 시드
  값을 가정해서 한 번 죽으면 다음 판까지 붉어지던 브라우저 시험. 셋 다
  지키려던 것을 실제로 지키도록 다시 썼습니다.

## 1.0.1 — 2026-09-12

고치는 판입니다. 새 기능은 없습니다. 전체를 다시 읽으면서 찾은 것들이고,
**하나같이 "주석·문서가 약속한 것과 코드가 다르다"** 한 종류였습니다.
고친 자리를 하나씩 되돌려 시험이 붉어지는 것을 확인했습니다.

### 올리기 전에

`alembic upgrade head` 를 돌립니다(`user.pre_suspend_status` 한 칸이
늘었습니다). 이관 묶음 형식과 API 는 그대로입니다.

### 보안

- **초대 토큰이 이미 활성화된 계정의 비밀번호를 덮어썼습니다.** 토큰은 DB 행
  없이 암호화만 해서 주므로 만료(7일) 전까지 몇 번이든 통합니다. 전달된 메일,
  공용 메일함, 링크가 남은 브라우저 기록 어디서든 새면 그 계정을 통째로
  가져갈 수 있었고, 본인 세션은 끊기지도 않아 당한 쪽은 눈치채지 못했습니다.
  이제 `invited` 인 계정만 초대를 수락합니다.

  그 문을 닫으면 **비밀번호를 잊은 사람이 돌아올 길이 없어집니다**(재설정
  흐름이 아직 없습니다). 그래서 관리자가 여는 문을 같이 만들었습니다 —
  사용자 관리에 "초대 다시 보내기" 가 생겼습니다. 계정을 `invited` 로
  되돌리고, 세션을 끊고, 옛 비밀번호를 지우고, 초대장을 새로 보냅니다.
  IdP 가 관리하는 계정에는 거절합니다.

- **이슈 보안 레벨이 목록·검색·내보내기로 통째로 샜습니다.** 단건 조회는
  403 인데 이슈 목록·IQL 검색·저장 필터·CSV 내보내기·보드·간트·캘린더·
  리포트·데스크 큐에는 요약이 그대로 실렸습니다. 객체 수준 관문이 단건
  조회에서만 도는 구조였고, 목록 질의에는 같은 규칙이 없었습니다.

- **구독에 권한 검사가 없었습니다.** 알림 제목에는 이슈 키와 요약이, 본문에는
  바뀐 내용이 담겨 나갑니다. UUID 하나만 알면 남의 프로젝트·이슈·문서를
  구독해 그 소식을 받아 볼 수 있었습니다. 이제 볼 수 있는 것만 구독됩니다.

- **중첩된 문서 열람 제한이 검색 색인에서 느슨해졌습니다.** 실제 열람 판정은
  조상 사슬을 **전부** 통과해야 하는데, 색인에는 가장 가까운 제한 하나만
  적었습니다. `/A` 는 갑만, `/A/B` 는 을만 볼 수 있게 해 두면 을은 문서를 못
  여는데 검색 결과에는 제목과 본문 발췌가 떴습니다.

- **제한 걸린 문서의 사본이 검색에 그대로 떴습니다.** 사본을 만든 뒤에 제한을
  옮겼는데, 색인은 만드는 시점에 이미 쓰였습니다.

- **전역 웹훅이 프로젝트 권한만 있는 사람에게 URL 채로 보였습니다.** 목록에는
  주소가 그대로 실리는데, 조직 전체에 걸린 연동 주소에는 대개 경로나 질의에
  비밀이 섞여 있습니다(Slack·Teams 의 수신 주소가 그렇습니다). 단건 조회는
  이미 스코프를 제대로 봤고 목록만 넓었습니다.

- **위키 ZIP 가져오기가 압축된 크기만 쟀습니다.** deflate 는 0 으로 채운
  파일을 1000:1 넘게 줄이므로, 상한(50MB)을 통과한 작은 ZIP 하나가 수십 GB
  로 풀려 프로세스를 메모리로 죽일 수 있었습니다. 이제 푸는 동안 누적
  해제량을 셉니다.

### 데이터를 잃던 것

- **아웃박스에서 한 건의 DB 오류가 배치 전체를 되돌렸습니다.** 행마다
  `except` 가 있었지만 DB 오류는 세션을 못 쓰게 만들고, 그래서 재시도 횟수를
  적는 것까지 같이 되돌아갔습니다. 같은 배치의 멀쩡한 이벤트가 사라졌고,
  재시도 상한이 영원히 안 걸려 같은 행이 배치마다 되돌아왔습니다.
  이제 한 건씩 세이브포인트로 감쌉니다.

- **알림 제목이 열 상한을 넘으면 드레인이 통째로 죽었습니다.** 제목 템플릿은
  이슈 요약을 통째로 품는데(`{key} 생성됨: {summary}`) 요약도 제목 열과 같은
  500자까지 받습니다. 긴 요약 하나가 그 배치의 알림을 전부 날렸고, 위의
  아웃박스 문제와 겹쳐 그 상태가 계속됐습니다. 이제 열 너비에 맞춰 자릅니다.

- **받은 메일 한 통이 죽으면 나머지가 사라졌습니다.** "한 통이 죽어도 앞의
  것을 잃지 않는다" 고 적어 두고, 읽을 수 없는 메일만 감쌌습니다. 읽히는데
  처리에서 죽는 메일이 있으면 예외가 반복문 밖으로 나갔고, 그 메일들은 IMAP
  에서 이미 읽음으로 표시돼 다시 가져올 수 없었습니다.

- **IQL 목록의 2페이지부터 행이 조용히 사라졌습니다.** 커서는 `id` 기준인데
  정렬은 IQL 이 정합니다. `ORDER BY` 가 붙으면 다음 페이지가 아직 안 보여 준
  행 중 절반쯤을 버리고 이미 보여 준 행을 다시 실었습니다. 정렬이 붙은
  질의는 자리 수로 셉니다(정렬이 없는 흔한 경우는 그대로 id 커서입니다).

### 일이 어긋나던 것

- **`assignee WAS NOT x` 가 미배정 이슈를 통째로 숨겼습니다.** SQL 의 세 값
  논리 때문입니다 — 담당자가 없으면 비교가 NULL 이고 `NOT NULL` 도 NULL 이라
  그 행이 조건에서 빠집니다. "그 사람을 거친 적 없는 일" 을 찾는 질의가 제일
  먼저 나와야 할 것을 빼놓았습니다.

- **창 경계에 날짜 리터럴과 날짜 함수를 섞으면 500 이 났습니다.**
  `status CHANGED DURING (2026-01-01, now())` 처럼 씁니다. 문법도 필드도 맞는
  질의인데 400 이 아니라 500 이라, 에디터는 어디가 틀렸는지 못 보여 주고
  저장 필터에 넣어 두면 열 때마다 터졌습니다.

- **SSO·SCIM 계정을 정지했다 되살리면 영영 못 들어왔습니다.** 되살릴 상태를
  비밀번호 유무로 갈랐는데, 비밀번호가 없는 계정은 두 가지입니다 — 수락 안 한
  초대와 IdP 계정. 후자가 `invited` 로 떨어져 SSO 버튼이 401 을 돌려줬고,
  다시 초대하려 하면 이메일이 있다고 409 였습니다. 이제 정지 직전 상태를
  기억합니다. (옛 판에서 이미 정지된 계정은 마이그레이션이 가진 증거로
  채웁니다.)

- **위키 스페이스·데스크 큐에 역할을 줄 길이 아예 없었습니다.** `org.role.assign`
  권한이 전역·프로젝트 스코프만 받도록 정의돼 있었고, 정의에 없는 스코프로
  물으면 거절이 아니라 500 이 납니다. 관리 콘솔에서 스페이스 권한을 주려는
  요청은 전부 거기서 죽었습니다. 겸해서, 스코프에 맞지 않는 권한이 든 역할은
  **만들 때도** 거절합니다 — 고칠 때만 보고 있어서 같은 역할이 만들 때는
  통과하고 고칠 때만 거절되는 상태였습니다.

- **워커를 여럿 띄우면 같은 SLA 클럭을 둘이 집었습니다.** 위반·에스컬레이션
  스윕만 행을 안 잠갔습니다 — 위반 알림이 두 통 가고 에스컬레이션이 두 번
  돌았습니다.

- **스윕의 앞 단계 하나가 터지면 뒤가 통째로 굶었습니다.** 아웃박스가 막히면
  SLA 시계도, 받은 메일도, 반복 이슈도 같이 멈췄습니다. 이제 단계마다
  격리되고, 무엇이 터졌는지는 심장박동에 적힙니다.

### 이관

- **우선순위를 이름으로 잇습니다.** 예전에는 묶음에 나온 순서로 폈는데, 그
  순서는 소스가 정한 것이 아니라 **이슈가 나온 순서**였습니다. 첫 이슈가
  High 면 High 가 가장 낮은 자리로 갔습니다. Redmine·Jira 의 기본 이름과 그
  한국어 이름을 알아보고, 하나도 못 알아보면 예전처럼 순서로 짐작합니다
  (화면이 "짐작했다" 로 표시합니다).

- **상태 이름 앞뒤에 공백이 있으면 적재가 500 으로 죽었습니다.** 어휘를 모을
  때는 다듬고 찾을 때는 원본을 썼습니다. 미리 보기는 통과시켜 놓고 이슈를
  절반쯤 만든 뒤에 터졌습니다.

- **다른 워크플로우의 상태로 이관되는 것을 미리 막습니다.** 상태 목록은
  프로젝트의 모든 워크플로우를 한 통에 담아 보여 줍니다. 워크플로우가 둘
  이상이면 이름만 보고 고른 상태가 다른 워크플로우의 것일 수 있고, 그대로
  실으면 그 상태에서 나갈 전이가 하나도 없는 — 아무도 못 옮기는 이슈가
  됩니다. 미리 보기가 이제 그것을 막습니다.

### 아직 안 된 것

- 비밀번호 재설정을 **본인이** 하는 흐름은 아직 없습니다. 관리자의 "초대 다시
  보내기" 가 그 자리를 대신합니다.
- 구독은 **구독하는 순간에만** 권한을 봅니다. 구독한 뒤에 그 프로젝트 접근이
  회수되면 알림은 계속 갑니다. 발송 시점 검사는 다음 판의 일입니다.

## 1.0.0 — 2026-09-11

첫 정식 판입니다. 이슈·위키·서비스데스크가 **같은 사용자·권한·검색·알림
위에** 있습니다.

### 들어 있는 것

**이슈** — 프로젝트 계층, 커스텀 필드(종류별 위젯), 워크플로우(전이 조건·
필수 필드), 시간 추적, 이슈 관계와 하위 작업 진행률 롤업, 일괄 편집,
CSV 내보내기, 반복 이슈, 첨부(브라우저에서 스토리지로 직접 업로드).

**계획** — 칸반 보드(스윔레인), 스프린트와 번다운, 간트, 캘린더.

**위키** — 스페이스와 문서 트리, 판 비교, 인라인 코멘트(퍼지 앵커·고아 처리),
템플릿, 매크로(`::toc` `::children` `::issues` `::chart` `::excerpt` `:::info`),
동시 편집(실시간 커서·프레즌스), 개인 초안 자동 저장, `.md` 가져오기·
내보내기(첨부 포함 ZIP), PDF·Word 조판, 스페이스 블로그, 본문 체크박스를
할 일로 세기.

**서비스데스크** — 고객 포털과 요청 유형, 큐(IQL 로 정의), 내부 노트와 고객
회신 분리, 정형 응답, SLA(업무 달력·멈춤 상태·에스컬레이션), 이메일 채널
(수신 파싱·스레드 매칭·반송), KB 추천, 자동화 규칙, CSAT, 승인 관문,
자산(CMDB).

**찾기** — 이슈와 문서 통합 검색(PGroonga 한국어 형태소), 질의 언어 IQL
(이력 연산자 `WAS`·`CHANGED` 포함), 자동완성과 오류 위치 표시, 저장 필터와
URL 공유, IQL 집계 리포트, 데스크 성과 리포트.

**키보드** — 전역 명령 팔레트(`Cmd/Ctrl+K`), 이슈 생성 `c`, 검색 칸으로 `/`,
단축키 도움말 `?`, 목록 훑기 `j`/`k`/`o`/`Enter`, 이슈 상세의 `e`(편집)·
`a`(담당자)·`s`(상태 전이)·`p`(우선순위). 글을 쓰는 중에는 한 글자 키가
꺼지고, 덮개가 떠 있는 동안에는 뒤의 단축키가 쉽니다. 도움말은 **그 화면에서
실제로 도는 것만** 보여 줍니다.

**인증과 관리** — OIDC(PKCE) 및 SAML 2.0 SSO, JIT 프로비저닝과 그룹 매핑,
SCIM 2.0, TOTP·패스키(WebAuthn) 2FA 와 조직 강제 정책, 민감한 설정의 step-up
재확인, 역할·권한(전역·프로젝트·스페이스·큐 네 스코프), 객체 단위 열람 제한,
감사 로그, 세션 개별 폐기, 고객 계정 격리.

**잇기** — 웹훅(HMAC 서명·재시도·자동 중단·전송 로그), 개인 액세스 토큰,
앱 슬롯(코드가 아니라 데이터를 받는다), 코드 저장소 연동(웹훅 수신).

**옮겨 오기** — Redmine 에서 이슈·코멘트·부모·관계를 가져옵니다. 뽑는 쪽은
관리자 기계에서 도는 표준 라이브러리 전용 꾸러미이고, 받는 쪽은 앱 안입니다 —
둘을 가르는 것은 묶음 포맷이라 다른 소스를 더할 때 받는 쪽은 손대지 않습니다.
**소스의 낱말은 이름이 같은 것만 잇고 나머지는 사람에게 묻습니다** — 번역표를
넣으면 그 표가 맞는 설치본에서만 맞고 틀린 설치본에서는 조용히 틀린 자리로
들어갑니다. 짝을 못 지은 것이 있으면 적재가 막히고, 이력(만든 시각·작성자)은
소스의 것을 그대로 씁니다. 두 번 돌려도 안 늘어나고, **못 옮긴 것을 화면이
접어 두지 않습니다.**

**운영** — `/healthz`·`/readyz` 분리, Prometheus `/metrics`, 워커 심장박동,
아웃박스 지연 지표, Helm 차트, 검색 백엔드 교체(PGroonga ↔ OpenSearch),
한국어·영어 UI 전체.

**깔기** — `deploy/install/install.sh` 가 한 줄로 세웁니다. 물어보는 것은
**주소 하나**이고, 비밀 키·DB 비밀번호·스토리지 열쇠는 그 자리에서 만들어
`.env`(0600)에 넣습니다. 스키마 올리기와 시드가 앱보다 먼저 돌고, 다 건강해진
뒤에 주소와 첫 비밀번호를 찍습니다. 판 올리기도 같은 명령입니다 — 비밀은 다시
만들지 않고 `compose.yml` 만 그 판의 것으로 갈아 끼웁니다.

**밖으로 여는 포트는 하나**입니다. 화면·API·첨부가 같은 오리진으로 나가서 앞에
TLS 를 세울 때 붙일 곳이 한 군데입니다. 이미지는 빌드하지 않고
`ghcr.io` 에서 당깁니다 — 깐 사람과 우리가 같은 바이트를 돌아야 "무엇이
돌고 있냐" 에 답할 수 있기 때문입니다.

### 어디까지 실제로 확인했는가

**이 목록이 이 릴리스 노트의 요점입니다.** "구현했다" 와 "실제로 그렇게
돈다" 는 다르고, M6 에서 그 차이로 고장 열 개 이상을 찾았습니다.

확인한 것:

- **브라우저로 몰아 본 것** 267개 시험. 실제 Postgres·Redis·MinIO·SMTP 를
  띄우고 진짜 크로미움으로 화면을 씁니다. 콘솔 오류가 하나라도 나면 붉어집니다.
- **키보드만으로 몰아 본 것.** 이슈를 만들고, 목록에서 찾아 들어가고, 상태를
  전이하고, 코멘트까지 마우스 없이 갑니다. 단축키가 **안 먹는 것**보다 **글
  쓰는 중에 튀는 것**이 더 나쁘므로(쓰던 것을 잃고 이유도 모릅니다), 판정부
  단위 시험 22개 가운데 아홉이 "안 먹어야 하는 자리" 입니다 — 입력 칸·선택
  상자·서식 편집기·IME 조합 중, 그리고 덮개가 삼킨 키.
- **API 시험** 2,888개(2,875 통과 / 13 건너뜀). 통합 시험은 실제 PGroonga
  이미지를 씁니다 — 확장 유무로 CI 만 통과하는 일이 없게.
- **인스턴스를 둘 띄운 것.** 세션·토큰·편집 표·step-up·로그아웃·refresh
  재사용 탐지가 인스턴스를 넘습니다. 동시 편집도 프로세스를 넘습니다.
- **워커를 둘 띄운 것.** 다이제스트 메일이 워커 수만큼 나가지 않습니다.
- **진짜 OpenSearch 를 세운 것.** 두 검색 백엔드에 같은 질의를 던져 결과와
  권한 필터가 같은지 봅니다.
- **백업을 되돌려 본 것.** 행 수, 확장 유무, 한국어 검색이 실제로 도는지까지.
- **프로브를 끊어 본 것.** 스토리지를 끊으면 `/readyz` 가 503, `/healthz` 는
  그동안 200.
- **이미지를 돌려 본 것.** 빌드한 컨테이너 안에서 `/openapi.json` 이 이 판을
  말하는지 봅니다. 여기서 실제로 하나 찾았습니다 — 앱이 자기 판을 `0.0.0`
  이라고 말하고 있었고, 개발 트리의 시험은 통과하고 **배포되는 것만** 틀렸습니다.

**한동안 CI 가 새 잡에 러너를 붙이지 않았습니다.** 저장소가 private 이라
Actions 분이 과금 대상이었고, 2026-09-11 04:05 부터 큐에 든 잡이 단계 하나 없이
몇 초 만에 끝났습니다. 그 사이의 커밋은 같은 게이트를 **로컬에서** 돌려
검증했습니다 — 전체 브라우저 스위트 **267 통과 / 0 실패**.

저장소를 공개로 돌리면서 풀렸습니다(public 은 Actions 가 무료입니다). 이 판은
그 뒤에 CI 위에서 나갔습니다.

설치본은 **레지스트리에서 진짜로 당겨서** 확인했습니다. 먼저 같은 이름으로
로컬 레지스트리에 올리고 캐시를 비운 뒤 깔았습니다 — 당기기·스키마 올리기·
시드·건강 대기·첫 로그인·첨부 왕복·동시 편집 소켓까지 한 번에.

판이 나간 뒤 **ghcr 에서 직접** 다시 확인했습니다: `ghcr.io/pan889/ieum-api:1.0.0`
과 `ieum-web:1.0.0` 이 **로그인 없이** 당겨지고, 당긴 API 이미지가 자기 판을
`1.0.0` 이라고 말합니다.

**확인하지 못한 것** — 첫 배포는 이것을 전제로 다루세요:

| | |
|---|---|
| 진짜 쿠버네티스 클러스터 | 매니페스트 검증(`--dry-run=server`)·이미지·프로브까지는 봤지만 **파드가 Ready 되는 것은 못 봤습니다**(개발 샌드박스의 중첩 컨테이너 제약) |
| 인스턴스 셋 이상 | 확인한 것은 둘입니다 |
| 쿠버네티스 인그레스의 웹소켓 설정 | 컴포즈 쪽은 확인했습니다 — 운영 이미지의 nginx 를 지나 동시 편집 소켓이 붙는 것을 재고, 업그레이드 헤더를 빼면 붉어지는 것까지 봤습니다. 인그레스 컨트롤러는 배포마다 달라 안 봤습니다 |
| 목표 규모의 성능 | 이슈 50만·문서 10만·p95 목표는 **목표**입니다. 그 규모로 재 보지 않았습니다 |
| 큰 검색 색인 | OpenSearch 로 옮겨 본 것은 5천 문서입니다 |
| 보안이 켜진 OpenSearch | 사용자·비밀번호 설정은 있지만 인증을 요구하는 클러스터로는 안 돌려 봤습니다 |
| 파드가 갑자기 죽을 때 | 정상 종료는 편집을 지킵니다. SIGKILL 에서는 최대 3초가 사라집니다 |

### 빠진 것

- **Jira·Confluence·Zammad 어댑터.** 이관은 Redmine 만 됩니다. 뽑는 쪽과
  받는 쪽을 가르는 것이 묶음 포맷이라, 다른 소스를 더할 때 받는 쪽은 손대지
  않습니다.
- **프로젝트 목록 밖의 구독 손잡이.** 이슈·프로젝트·문서·스페이스를 구독할 수
  있지만, 프로젝트는 전용 상세 화면이 없어 목록에서만 켭니다.
- 승인 뒤 자동 전이, 고객 포털에서의 승인.
- 서식 모드의 원격 커서, 갈라진 공유 초안의 3-way 병합.
- 자산 종류·속성을 데이터로 정의하는 JSM 식 Assets.
- `issue.badge` 앱 슬롯(목록에서 N+1 을 만들지 않는 일괄 로딩이 먼저).

### 라이선스

AGPL-3.0-only 입니다.
