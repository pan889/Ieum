# 크롤러에게. 막는 것은 없다 — 이 사이트는 전부 읽히라고 낸 것이다.
#
# **지금은 이 파일이 아무 일도 안 한다.** 크롤러는 robots.txt 를 **도메인
# 뿌리**에서만 읽는데, 이 사이트는 `pan889.github.io/Ieum/` 라는 하위 경로에
# 있어서 우리를 지배하는 것은 `pan889.github.io/robots.txt` 다. 그 자리는
# 지금 404 이고(뿌리 사이트가 없다), 404 는 "다 허용" 으로 읽힌다. 그래서
# 막히는 것은 없다.
#
# 그럼 왜 두는가: 나중에 **자체 도메인**을 붙이면 그 순간 이 파일이 뿌리가
# 되어 실제로 동작한다. 그때 가서 만들면 사이트맵 줄을 빠뜨린다.
User-agent: *
Allow: /

Sitemap: https://pan889.github.io/Ieum/sitemap.xml
