# 매뉴얼 사이트를 빌드하는 데 필요한 것. **판을 고정한다** — 문서가 어제와
# 다르게 그려지는 이유가 "테마가 올라갔다" 이면 아무도 못 찾는다.
#
# Material 은 MkDocs 2.0 이 플러그인·테마를 통째로 깨뜨린다고 경고하고 있다
# (설치할 때 그 경고를 찍는다). 우리는 1.x 를 쓰고 고정해 두었으므로 지금
# 문제는 아니지만, 올릴 때는 그 경고를 먼저 읽는다.
mkdocs==1.6.1
mkdocs-material==9.7.7
